<?php 
        echo "PHP assignment by: Raymond Kolen (July Check)";

        date_default_timezone_set('UTC');

      if (date('F') == "July") {
        echo "<br>It's July, so it can get really hot.";
        } else {
          echo "<br>It’s not July, so at least we’re not in the peak of the heat.";
      }
      ?>